# Server-side only. Never expose this to the browser / never commit a real key.
ANTHROPIC_API_KEY=

# Public store configuration (safe to expose to the browser)
NEXT_PUBLIC_STORE_NAME=BudgetBazaar247
NEXT_PUBLIC_WHATSAPP_NUMBER=966500000000
NEXT_PUBLIC_SITE_URL=http://localhost:3000
